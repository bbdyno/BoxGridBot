# BoxGridBot — 작업 맥락 (Claude 용 인수인계)

개인용 비트코인 매매 판단 봇. 업비트 BTC/KRW 현물, 롱 전용, 텔레그램으로 제어·알림. 상세 규칙은 `README.md`, 기기 이전은 `MIGRATION.md`.

## 현재 단계 (2026-09-21)
- **실거래는 홀드.** `config/grid.yaml` `live_lock: true`. 페이퍼(모의)로 판단·알림만 하며, 사용자가 판단 품질을 지켜보는 중이다.
  실거래 연결은 사용자가 명시적으로 요청할 때만: `live_lock: false` + 거래소 키(출금 권한 없음, IP 화이트리스트) + 텔레그램 "라이브 승인" 버튼. 첫날 시드 10% 상한.
- launchd(`com.boxgrid.bot`)로 상시 가동. **수동으로 `python -m boxgrid.main` 을 돌리기 전에 반드시 launchd 를 내린다**(텔레그램 폴링 409 충돌).
- 2026-09-15 눌림에서 1~3단 모의 체결, 이후 상승. 첫 실전 판단 사례.

## 사용자 선호
- 앱(iOS/Android)은 안 만든다. 봇으로만. 카카오톡은 발신 전용(나에게 보내기), 제어는 텔레그램.
- 알림은 **사람이 읽고 판단할 수 있는 문장**이어야 한다: 무슨 일 · 왜 · 다음에 볼 것. 금액은 억/만원, 약어(P1, SMA, ATR) 금지. `notify/humanize.py`.
- 사용자는 업비트 "코인 모으기"로 매일 2만원 적립 중. `/dca` 지표가 추가 매수 여부를 알려준다.
- 한국어. 코드 주석·알림 문자열도 한국어.

## 검증하고 나서 채택한다 (중요)
그럴듯한 규칙도 백테스트에서 지면 기본값으로 넣지 않는다. 지금까지의 결과:
| 아이디어 | 결과 | 처리 |
|---|---|---|
| 박스 하단 4단 그리드 + 일봉 종가 손절 | 2년 +15.3%, 보유 −10.6%, MDD 9.4% | 기본 전략 |
| 현재가 위 레벨은 현재가 −0.3% 아래로 게시 | 즉시 시장가 체결 방지, 수익 +5%p | 채택 |
| DCA: 상승 추세 + 눌림 점수로 추가 매수 | 2/4/6년 모두 평균단가 **상승** | 폐기 |
| DCA: 200일 평균 대비 할인폭으로 추가 매수 | 평균단가 −5.1/−8.1/−11.5% | 채택 |
| adaptive: 발산 장세엔 고점 기준 눌림 매수 | +4.8%, MDD 13.7% (변형 5종 모두 박스보다 나쁨) | 코드만 두고 기본 꺼짐. 장세는 메시지에 참고 정보로만 표시 |

표본이 작다(2년, 청산 7건). 파라미터를 더 깎아 맞추지 않는다.
검증 명령: `scripts/backtest.py --days 730`, `scripts/dca_eval.py`.

## 구조 원칙
- `strategy/` 는 순수 로직(거래소·시계·저장소 접근 없음). `grid.py` 가 Decision 목록을 돌려주고 `core/engine.py` 가 실행한다. 그래서 시계를 고정한 테스트로 모든 분기를 검증한다.
- 손절은 일봉 확정 종가(09:00 KST)로만. 장중엔 재난 손절(SL −3%)만. 이 구분이 전략의 핵심이므로 바꾸지 않는다.
- 새 기능은 테스트와 함께. `.venv/bin/python -m pytest -q` 전부 통과 유지.
- 공개 저장소다. 토큰·chat_id·DB·영상 자막 원문을 커밋하지 않는다.

## 전략 출처
유튜브 차트 분석 영상 두 편을 규칙화했다. 요약과 채택/기각 내역은 `docs/VIDEO_NOTES.md`.

## 다음 후보
- 페이퍼 판단을 2~4주 더 지켜본 뒤 실거래 연결 여부 결정
- 카카오 "나에게 보내기" 복제 발송(OAuth 1회 필요, 미검증)
- 주 1회 봇 자동 재기동, 하트비트 끊김 알림
